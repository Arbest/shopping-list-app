// Configuration for API mode
// Set USE_MOCK to true to use mock data instead of real API
export const USE_MOCK = true;

// API base URL
export const API_BASE_URL = 'http://localhost:3001';

// Current user ID (for authentication header)
export const CURRENT_USER_ID = 'u1';
export const CURRENT_USER_NAME = 'Pavel Arbes';
